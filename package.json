{
  "name": "GRX-turborepo",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.2.4",
    "prettier": "^3.6.2",
    "turbo": "^2.8.3",
    "typescript": "5.9.2"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "pnpm@10.20.0",
  "pnpm": {
    "overrides": {
      "esbuild": "0.26.0"
    }
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
